<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout style="@style/LayoutHoriz">
        <ImageView
            android:id="@+id/image"
            android:layout_width="@dimen/ImgWidth"
            android:layout_height="@dimen/ImgHeight"
            android:layout_gravity="center_vertical"
            android:layout_alignParentLeft="true"/>
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:layout_toRightOf="@+id/image"
            android:layout_marginLeft="@dimen/TxtViewMarginLeft">
        <TextView style="@style/TxtLabel"
            android:id="@+id/name_text_view"
            android:text="Location name"/>
        <TextView style="@style/TxtView"
            android:id="@+id/description_text_view"
            android:text="Description"/>
        <LinearLayout style="@style/LayoutHoriz" >
            <TextView style="@style/TxtLabel"
                android:text="Address:"/>
            <TextView  style="@style/TxtView"
                android:id="@+id/address_text_view"
                android:text="Address"/>
        </LinearLayout>
        <TextView style="@style/TxtView"
            android:id="@+id/timetable_text_view"
            android:text="Timetable"/>

        <TextView style="@style/TxtView"
            android:id="@+id/phone_text_view"
            android:text="Phone"/>

        <TextView style="@style/TxtView"
            android:id="@+id/website_text_view"
            android:text="Website"/>
        </LinearLayout>
    </LinearLayout>
</RelativeLayout>